# Local server port
PORT=3000

# Paste your Redis connection URL here (rediss://...), not the REST API URL.
# Leave blank to run locally in memory without Redis.
REDIS_URL=

# Keep local testing separate from the production race.
RACE_ROOM=local
